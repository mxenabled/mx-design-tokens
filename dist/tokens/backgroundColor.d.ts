import type { BackgroundColorType, TokenTypes } from '../TokenTypes';
declare const _default: {
    light: (core: TokenTypes) => BackgroundColorType;
    dark: (core: TokenTypes) => BackgroundColorType;
};
export default _default;
