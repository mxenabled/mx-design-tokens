import type { BackgroundColorType, TokenType } from '../TokenType';
declare const _default: {
    light: (core: TokenType) => BackgroundColorType;
    dark: (core: TokenType) => BackgroundColorType;
};
export default _default;
